ccxt==4.5.61
numpy==1.22.4
pandas==1.3.5
requests==2.27.1
python-dotenv==0.21.1
# 回测预热（backtest/）按天 parquet 缓存所需；与 pandas 1.3.5 / py3.9 兼容
pyarrow>=8.0.0,<13.0.0
SQLAlchemy>=2.0,<2.1
psycopg2-binary>=2.9,<3
# 回测选币回放复用 account_0 的因子计算，需要 TA-Lib（与实盘环境一致）
# TA-Lib  # 需先装系统库 ta-lib，再 pip install TA-Lib；实盘环境已具备
# Dashboard（web 进程；monitor/scheduler 不依赖）
fastapi>=0.110,<0.116
uvicorn[standard]>=0.27,<0.35
jinja2>=3.1,<4
python-multipart>=0.0.12,<0.1  # FastAPI Form() support
httpx>=0.23,<1  # starlette.testclient dependency
tzdata  # zoneinfo 时区数据源（DISPLAY_TZ 非 UTC 时 slim 镜像必需）
