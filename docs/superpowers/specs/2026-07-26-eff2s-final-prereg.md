# eff2s 双窗终审 · 预注册（判据先于数据）

**日期**：2026-07-26（写于两个裁决窗任何数据构建之前）
**候选**：#12 · eff2s——九斩后唯一"死于平庸而非爆仓"者（eff1）的既有药方修复版。

## 1. 候选定义（冻结，禁调）

```
第一阶段: rank(cross1_p12) ≥ 当轮池 P25       ← 燃料否决门,借 2026-07-22 评价分工具
                                                文档既有值(E单死水病历),非新参数
第二阶段: 合格集内按 p12_eff = cross1/(1+100·mae) 降序取 top-1
```
- cross1/mae 均为过去 12h 实现标签（rt+12h=R 平移，PIT 语义同 p12 注入路径）。
- **单臂**：链 = s030 生产现值全套（隔离选币器效应；不跑链家族——第 12 次尝试的全部
  自由度预算 = 一个借来的 P25 + 一个排序，到此为止）。
- 锚臂 = 现役 rank_sum×s030（各窗逐位复现/真实盘记录）。

## 2. 裁决窗（选窗规则先声明，禁止窗口购物）

- **HOLD-F = 2024-08-01 ~ 2024-09-30**：规则="2024-10 之前、距今最近、Vision 可完整
  下载的连续两个月"——由规则唯一确定，非挑选。本地归档零覆盖（最早 2024-09-16），全新下载。
- **JUL26 = 2026-07-03 ~ Vision 已发布末日（约 07-23，含因子暖机后有效段）**：
  前向影子第一期回填。约 480 全轮 ≈ 标准窗 1/3，统计上弱窗——但最差窗规则里它只需
  "不败"即可发挥作用。
- 两窗与全部 27 臂/70+ 探针的设计与验证数据**零重叠**（历史窗止于 2026-06-30；
  2024-10 前从未触碰）。

## 3. 判据（最差窗规则，写死不挪）

主判据（**两窗均须**，组合口径）：
```
Calmar(eff2s) ≥ Calmar(锚)  且  MDD(eff2s) ≤ MDD(锚)×1.3  且  ret(eff2s) > ret(锚)
```
副检验（同报不裁决）：两窗合并逐轮配对 alpha；退化遥测=选中格燃料分位中位 ≥ P40
（分母劫持复发探测器）。任一窗 FAIL → 候选 #12 终局，斩杀率记 10/10，选币器线永久关闭
（下一次立项需全新信息源，不再是因子重组）。

## 4. 污染披露（如实records）

- 锚臂的三因子（Reg/Sgcz/Er）系 legacy 量化遗产，原始调参年代可能触及 2023-24 数据
  → 锚在 HOLD-F 有主场优势 = 对候选的保守偏置（可接受方向）。
- P2 巡检曾计算 2026-07-19~21 三天反事实（账本锚用途，非选币设计）。
- eff2s 的修复设计源于 HOLD-E 尸检 → HOLD-E 及九个历史窗对本候选永久只作探索参考。

## 5. 执行顺序（战役 session）

① 本文件 commit 后才许动数据 ② Vision 下载 HOLD-F（1m/1h/funding，全 universe）+
补齐 JUL26 ③ 标签 stage L + 因子 stage F + POOL 回放 ④ 锚臂复现（JUL26 可对
selection_snapshots 真实盘选择交叉验证）⑤ 两窗双臂出数 → 按 §3 判 ⑥ 先报数再记账。
