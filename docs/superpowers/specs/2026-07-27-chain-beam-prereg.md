# 链轴束搜索(从生产现值出发)· 预注册 v2

**日期**：2026-07-27，**写于第二次尝试的任何新臂出数之前**。
**几何冻结** `b3_c16`（几何轴已由 `2026-07-26-geo-chain-joint-prereg` 判死并回到保现值）。

---

## 0. 披露：这是**第二次**尝试

第一次（K1，起点 `pv_mult=5`）**失败并已终止**，须计入多重检验负担：

```
臂        九窗ret   判定4    留出5   单窗MDD≤现值×1.3
s1.0_m5   +8.54    +9.75   −1.20   通过    ← 唯一存活,但劣于现值 +28.03 且留出为负
s1.5_m5  +22.91   +14.39   +8.52   W1 越限
s2.0_m5  +26.32   +18.59   +7.73   W1 越限
s2.5_m5  +36.52   +26.17  +10.34   W1 越限
s3.0_m5  +37.45   +28.38   +9.06   HOLD-A/B 越限
```

失败的**两个原因**（都已修，见 §2/§3）：
1. **规则缺"现值地板"** —— 首段无基座臂，故未拦住一个劣于生产现值的点入束。
2. **单窗 MDD 硬帽太脆** —— 三个臂只毙在 W1 一窗上，其中 `s2.5_m5` 的留出五窗
   是全场最好（+10.34）。用户令改用 Calmar 约束。

**已消耗**：K1 五点。累计搜索预算从此计入（见 §5）。

---

## 1. 搜索空间（冻结）

**轴与取值**（其余链参数 = s030 生产现值）：
```
stop_loss       {1.0, 1.5, 2.0, 2.5, 3.0}%     现值 3.0
pv_thr          {−0.5, −1.0, −2.0}%            现值 −1.0
pv_mult         {3, 5}                          现值 3
trailing_floor  {1, 2, 4}%                      现值 2
trailing_k      {0.15, 0.30}                    现值 0.30
funding_stop    {0.15, 0.3, 关}%                现值 0.15
```

**分段（束宽 B=3）**：
- **S0 种子**：生产现值 + 全部单轴偏移 = **14 个配置**。
  **无需重跑** —— T1 重扫的 `P1/geo_b3_c16` + `P3`(5) + `P5`(8) 已在九窗跑完，tick-clean 同口径。
- **S1 扩展**：束内每点沿**每个轴**取该轴全部值（去重、去已跑），约 3×12 ≈ 36 臂。
- **S2 扩展**：同上，再往外一层。
- 到 S2 为止（距现值编辑距离 ≤3）。**不再往外扩**。

---

## 2. 选点规则（写死于任何新臂出数之前）

按序，前一条不满足即淘汰：

1. **现值地板（第一次失败的修正）**：九窗合计 ret（密度修正后）**必须 > 生产现值**（+28.03）。
2. **Calmar 约束（用户令，取代单窗 MDD 硬帽）**：
   九窗合计 **对数 Calmar** ≥ 生产现值同口径（**≥ 50.23**）。
   > 用**对数**年化 `(365/days)·ln(1+ret)/MDD` 而非标准年化 `((1+ret)^(365/days)−1)/MDD`。
   > 二者在当前收益量级下**排序完全相同**（实测 K1 五臂 + 现值），但标准版
   > 在 ret 进入 O(1) 后退化为「带 6.19 次幂的收益排序」
   > （见 [[calmar-primary-metric-power-law-flaw]]）。同样的排序，少一个定时炸弹。
3. **灾难上限（保留一条硬安全）**：**无任何窗** MDD > 生产现值同窗 × **2.0**。
   > 比第一次的 ×1.3 放宽，但不取消 —— `b2_c26` 正是死在这条上（HOLD-B 超 82%）。
   > ×2.0 只拦真正的灾难，不再因单窗小幅超标而否决。
4. **束**：满足 1~3 者中，按九窗合计 ret 降序取前 **3**。
5. 某段无点满足 1~3 ⇒ **束保持不变**（该段不动）。
6. 全程无点满足 ⇒ **就地终止，保持现值，不进裁决**（HOLD-F/JUL26 不消费）。

每段同报三口径：九窗合计 / 判定四窗 / 留出五窗 —— 「判定封神、留出团灭」今日已见两次。

---

## 3. 裁决（HOLD-F / JUL26，唯一处女地，只看一次）

**候选 = S2 结束时束内最多 3 个 + 生产现值锚。**

判据（较 geo-chain 版加严，因搜索预算更大且这是第二次尝试）：
```
(i)   每窗逐轮配对差(候选−锚) 单尾 t > −1.65      —— 非劣性
(ii)  两窗合并逐轮配对 alpha t ≥ +2               —— 优越性
(iii) 每窗 MDD ≤ 锚 × 1.3                        —— 硬安全(裁决段收回 ×1.3)
(iv)  **多重比较**：3 候选 ⇒ (ii) 的门槛按 Bonferroni 提到 **t ≥ +2.5**
```
任一候选全过 ⇒ 该候选为终点；多个全过 ⇒ 取合并 t 最高者。
**全不过 ⇒ 保持现值，链轴方向就此关闭，不得第三次尝试。**

---

## 4. 污染披露

- **九窗（W1/W2/OOS/IS/HOLD-A~E）已全部消费**，S0 种子本身就来自其上的读数。
  「判定/留出」标签在本项目中**只剩诊断意义，不再是独立检验**。
- **HOLD-F / JUL26 零消费**，物理隔离（已从扫描脚本的窗表弹出，非仅靠 assert）。
- 本项目是链轴的**第二次**尝试；第一次（K1）已披露于 §0。

## 5. 搜索预算记账

```
T1 单轴 13(= S0 种子，复用)
K1 第一次尝试        5   ← 失败，仍计入
S1 扩展            ~36
S2 扩展            ~36
                 ≈ 90 点
```
裁决判据已按此加严（§3 的 iv）。**超出本清单的点位不得事后追加。**

## 6. 执行顺序

```
本文件 commit → S0 选点(纯读已有数据,不跑) → S1 → 选点 → S2 → 选点
  → 束(≤3)落盘 → **停,等人工审阅** → 裁决
```
每段先报数再记账。裁决段只看一次，看完不得回头改规则。
